# v1 needs no API keys: classification runs on local Ollama, and Gmail auth
# uses credentials.json / token.json files in the repo root (see README).
# Behavior settings live in config.toml.
#
# Reserved for the future cloud-provider swap (not read by v1):
# ANTHROPIC_API_KEY=
# OPENAI_API_KEY=
